---
title: 实体物品盘点系统产品与数据设计
type: project-design
status: active
created: 2026-08-10
updated: 2026-08-10
tags:
  - 项目
  - 物品盘点
  - 数据模型
---

# 产品与数据设计

返回 [[实体物品盘点系统]]。

## 实体模型

### 单件物品

用于需要独立追踪的耐用品、重要物、证件、衣物、工具、设备和容器。稳定键是 `entityId`，文件名和分类目录可以变化。

### 物品批次

用于同质消耗品或大量相同小件。标签确认这批物品或包装仍存在；`countMode: quantity` 时必须人工填写实际数量，NFC 或二维码本身不能自动知道余量。

### 标签绑定

一个有效标签绑定同时包含二维码和 NFC，并共享 `tagId`。标签更换时将旧绑定标记为 `replaced`，新标签使用新的 `tagId`，历史不覆盖。

### 地点、空间与容器

地点可以是现实地理地点，也可以是宿舍、酒店房间、客厅等用户定义对象。空间和容器形成可嵌套位置链，例如：`家 → 次卧 → 书桌抽屉 → 线材袋`。

## 扫描统一接口

```text
ScanObservation
- tagId: string
- method: nfc | qr | self_device | manual | uhf
- observedAt: ISO-8601 datetime
- auditId: string
- deviceId: string?
- rawHardwareIdHash: string?
```

二维码、NFC 和未来 UHF 都只负责产生 `ScanObservation`；盘点引擎不依赖具体射频技术。相同 `auditId + tagId` 的重复观测只计一次。

## 盘点状态

| 结果 | 含义 |
|---|---|
| verified | 在预期范围内找到 |
| unobserved | 本次未扫描，不等于丢失 |
| misplaced | 扫到但实际地点与主档不符 |
| quantity_mismatch | 批次数量与预期不符 |
| skipped | 有明确原因的人工跳过 |
| unexpected | 扫到但不在本次冻结快照内 |

大型盘点开始时冻结 `expectedEntityIds`。暂停、恢复、临时新建物品都不能改变本次盘点的原始分母。

## 携带清单

携带清单模板保存必带、可选、要求数量和默认容器。每次行程复制为实例后可以临时增删，不反向污染模板。出发、到达、离开和中途复核共用同一套清单实例。

必带物品没有扫到时，必须选择：`left_here`、`not_needed`、`unscannable`、`lost` 或 `other`，才能以 `completed_with_exceptions` 结束。

## 文件一致性

- 应用首次索引和恢复时只读取 `50_实体/`；
- 本地 Room 数据库保存路径、修改时间、内容哈希和未完成会话；
- 写入前重新计算 Markdown 哈希，外部编辑后拒绝静默覆盖；
- 应用只替换 YAML frontmatter 中由系统拥有的字段，正文内容原样保留；
- 盘点记录为追加文件，不回写或重写历史记录。

## NFC 与二维码负载

NFC 第一条 NDEF 记录是 `tel:` 联系方式，第二条 external-type 记录保存稳定身份；随后可写入物品名称文本记录和 Android Application Record。物品名称只用于人类提示，扫描匹配仍只认 `tagId`。external-type 记录为：

```json
{"v":1,"tagId":"随机标签ID"}
```

二维码使用 vCard 3.0：

```text
BEGIN:VCARD
VERSION:3.0
FN:失物联系
TEL:+86...
NOTE:这是遗失物品，请联系失主
X-OBSIDIAN-CODX-TAG:随机标签ID
END:VCARD
```

应用必须在激活前读回两种载荷并确认 `tagId` 一致。
